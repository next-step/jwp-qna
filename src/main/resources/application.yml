spring:
  datasource:
    url: jdbc:mariadb://localhost:33306/qna
    username: qna
    password: qna
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDB103Dialect
